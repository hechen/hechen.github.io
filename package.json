{
  "name": "hechen-github-io",
  "private": true,
  "version": "1.0.0",
  "description": "Build-time Tailwind CSS bundle for hechen.github.io's Hugo site. Compiled at build time so the rendered pages have no cdn.tailwindcss.com runtime dependency (which was the iPad fragility class of bug Phase 1 only fixed for blog posts).",
  "scripts": {
    "build:css": "npm run check:photos && tailwindcss -i ./assets/css/main.css -o ./public/css/tailwind.css --minify",
    "check:photos": "python3 scripts/test_photo_privacy.py && python3 scripts/photo_privacy.py --check static/gear static/images/gear"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "@tailwindcss/typography": "^0.5.15",
    "postcss": "^8.5.25",
    "postcss-cli": "^11.0.0",
    "autoprefixer": "^10.4.20"
  }
}
